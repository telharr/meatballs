{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "MEATBALLS Mod Catalog",
  "type": "object",
  "required": ["name", "items"],
  "properties": {
    "name": { "type": "string" },
    "ini_filename": { "type": "string" },
    "dedicated_workshop_items": {
      "type": "boolean",
      "description": "If false, apply-ini writes WorkshopItems= empty. Dedicated still loads Mods= from FTP folders; skips Steam download that NPEs B42 when a Workshop item fails."
    },
    "items": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["kind", "id"],
        "properties": {
          "kind": { "type": "string", "enum": ["mod", "library"] },
          "id": { "type": "string", "minLength": 1 },
          "name": { "type": "string" },
          "workshop_id": { "type": "string", "pattern": "^[0-9]+$" },
          "source": { "type": "string", "enum": ["local", "workshop"] },
          "path": { "type": "string" },
          "enabled": { "type": "boolean" },
          "notes": { "type": "string" }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
