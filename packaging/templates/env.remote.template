# Remote management client — generated by Inno Setup (Remote mode)
# Fill credentials locally. Never commit this file with real passwords.

AUTH_DISABLED=false
AUTH_LOCAL_BYPASS=false
PANEL_HOST=127.0.0.1
PANEL_PORT=8000

# Required before exposing panel on the internet:
ADMIN_USER=admin
ADMIN_PASS_HASH=
JWT_SECRET=

# —— Remote server (edit after install) ——
RCON_HOST=your.vps.example
RCON_PORT=16284
RCON_PASS=

FTP_HOST=your.vps.example
FTP_PORT=21
FTP_USER=
FTP_PASS=
FTP_REMOTE_DIR=/ServerWorld

# SFTP alternative (profile wizard also supports per-server secrets)
# SFTP_HOST=
# SFTP_PORT=22
# SFTP_USER=

PUBLIC_HOST=your.vps.example
GAME_PORT=16282
QUERY_PORT=16281
