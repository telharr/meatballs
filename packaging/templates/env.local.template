# Local desktop / developer — generated by Inno Setup (Local mode)
# Never commit real .env files.

AUTH_DISABLED=false
AUTH_LOCAL_BYPASS=true
PANEL_HOST=127.0.0.1
PANEL_PORT=8000
PANEL_OPEN_BROWSER=1

# Optional: local dedicated smoke test
# PZ_DEDICATED_DIR=C:\Program Files (x86)\Steam\steamapps\common\Project Zomboid Dedicated Server

# SteamCMD auto-installs to .cache/steamcmd/ on first Workshop download
