---
season: "00"
id: c01-gate
chapter: Дыра в караулке
version: 0.4.2
updated: 2026-09-05
handoff: mod-maker + pz-coder
---

# ТЗ: «Дыра в караулке» (`s00-c01`)

Источник кампании: [quests.md](../quests.md). Не переписывать хребет сезона этим файлом — это **спека одной главы** для спринта. Старая логика списка поручений: [_archive/0.3.1/](../_archive/0.3.1/).

Мод: новый `src/mods/MeatballsNadzor/42/` (B42). Не класть в `templates/mod/`. Префикс ModData: `MeatballsNadzor_`. World mutations только `server/`. Клиент шлёт intent.

---

## 1. Нарратив и диалоги

**Место выдачи:** Ash, ворота части `8080, 11750, z=0`. Сидит на ящике, грязная повязка, смотрит на пролом `8142, 11752`.

**Обоснование:** позавчера орда шла на свет фар. Рен завела грузовик в створку, чтобы пробить выезд. Дойл матерился. В проломе сквозняк с трассы и запах мертвечины. Если до сумерек не закрыть — ночью зубы на шее.

### Выдача

Ash не здоровается.

> Они шли на свет фар позавчера. Рен завела грузовик прямо в ворота, чтобы пробить заслон на выезде. Дойл матерился, но она не слушала. Теперь сюда тянет сквозняк с трассы и запах мертвечины. Если до сумерек не закроешь брешь — ночью мы проснемся с зубами на шее. За сараем лежат балки. Дотащишь — научу, как держать молоток, чтобы не отбить пальцы.

Сарай / балки: `8165, 11760, z=0`.

### Сдать без инструментов (нет молотка, или гвоздей < 4, или досок < 2)

Игрок жмёт «готово» / контекст створки / говорит Ash. Ash не принимает.

Нет молотка:

> Молоток. Не кулак. Не приклад. Молоток. Иначе пальцы будут как у Томми после лестницы.

Нет гвоздей (или < 4):

> Доски без гвоздей — это щель с лишним деревом. Четыре гвоздя. Не три. Не «потом донесу».

Нет досок (или < 2):

> Я сказал: сарай. Балки. Распил. Две доски на пролом. Пока таскаешь — не беги. Спина не казённая.

Перегруз (персонаж `tooHeavy` / вес выше максимума в момент сдачи или у створки):

> Поставь. Сейчас. Колени не казённые. Смена без хромого плотника ещё как-нибудь, без хромого новичка — тоже.

Повторная попытка сразу после отказа — короче:

> Инструменты. Потом разговор.

### Успех (створка забита, сервер подтвердил)

> Держит. Не красиво — держит. Сквозняк сдох. Запах нет. Иди к Марго, пока гвоздь из ворот не напомнил, что ржавчина тоже кусается.

### Ворота уже забиты (другой игрок закрыл пролом)

Мир: `s00_gate_repaired == true`. Этот персонаж ещё без флага главы. **Нельзя** оставить его на выдаче «тащи балки» — ремонта больше нет, застрянет до Марго.

Ash, один раз, затем выдаёт **тот же** переход, что и успех ремонта:

> Ворота уже забили до тебя. Не стой столбом, иди к Марго на кухню, там руки нужнее.

Сервер в этом диалоге **обязан** поставить игроку `MeatballsNadzor_s00_c01_done = true` (и алиас перехода к `s00-c02`). Журнал: `створка держит`. Жетон физически не выдавать.

Повторный клик:

> Марго. Кухня. Я не справочная.

### Жетон Кеннеди (осмотр / разговор после лута)

Предмет выдаётся **с сервера** в момент закрытия пролома: выпадает из паза, не лежит заранее на земле (иначе сперва лут, потом квест).

Ash, если игрок показывает жетон или открыт inspect:

> Не наш. Армия бросила блокпост у шоссе 11 июля. Рен притащила этот жетон с разведки. Сказала, помощи не будет, округ отписан под ноль.

Если игрок выбросил жетон:

> Подбери. Это не сувенир. Это приговор округу. Марго впишет, если доживёшь до книги.

Если жетон сгорел / ушёл с мёртвым телом, а в мире его нет (см. блок 4):

> Жетон уехал с карманом. Копия разведки — в столе офиса наверху. Пока офис закрыт, спроси Марго: у неё в ящике углярный лист. Не металл, но дата та же. 11 июля.

---

## 2. Спринт / ТЗ Lua и backend

Ветка работ: **`mod`**. Сценарий не мержить. Коммит: `feat(mod:MeatballsNadzor): s00-c01 gate repair`.

### Цели спринта (один PR-размер)

1. Интерактивный тайл пролома.
2. TimedAction заколачивания с проверкой инвентаря **на сервере**.
3. Замена спрайта / объекта створки, репликация клиентам.
4. Флаг игрока + выдача жетона один раз.
5. Перегруз балок с сарая не ломает сервер и не обходится «в кармане воздуха».

### Координаты

| Объект | X | Y | Z |
|--------|---|---|---|
| Ash (диалог) | 8080 | 11750 | 0 |
| Створка / пролом | **8142** | **11752** | **0** |
| Сарай, балки | 8165 | 11760 | 0 |

Радиус взаимодействия створки: персонаж на том же `z`, дистанция ≤ 2 тайла. Иначе отказ без анимации.

### Предметы

| Роль | FullType | Кол-во |
|------|----------|--------|
| Молоток | `Base.Hammer` | ≥ 1 (не съедать) |
| Гвозди | `Base.Nails` | ≥ **4** (съедать 4 при успехе) |
| Доски | `Base.Plank` | ≥ **2** (съедать 2 при успехе) |
| Балки у сарая | `Base.Log` и/или квестовая `MeatballsNadzor.ShiftBeam` (вес ≥ 8) | тащить к пиле / крафт в доски ванилью |
| Награда (первый ремонт) | `MeatballsNadzor.KennedyDogTag` | 1 на мир, не на игрока |
| Страховка (жетон потерян) | `MeatballsNadzor.KennedyFieldReport` | 1; стол офиса `8136,11730,z=1` и/или ящик кухни Марго |

Имя `Item_KennedyDogTag` в ТЗ заказчика = **DisplayName / внутренний id**. В скрипте B42:

```text
module MeatballsNadzor
item KennedyDogTag
```

Полный тип: `MeatballsNadzor.KennedyDogTag`. Иконка: заляпанный металл. `Weight = 0.1`. Не стек. `Tooltip` короткий: `KY NG · KENNEDY`.

`KennedyFieldReport`: бумага, `Weight = 0.05`. Текст «копия разведки, 11 июля». Не второй жетон.

### Проверки сервера (обязательны)

Клиент шлёт `sendClientCommand("MeatballsNadzor", "repairGate", { x=8142, y=11752, z=0 })`.

Сервер:

1. `player` жив, не nil, не в машине.
2. Квадрат существует, координаты совпадают (не доверять клиентскому «я починил»).
3. Флаг `MeatballsNadzor_s00_q01_gate_repaired` на **этом** игроке ещё false. (Мир: пролом общий — см. ниже.)
4. Инвентарь: `Hammer` ≥ 1, `Nails` ≥ 4, `Plank` ≥ 2. Считать контейнеры на поясе, не лут на земле.
5. Перегруз: если `player:getInventory():getCapacityWeight()` > `player:getMaxWeight()` — **не стартовать** TimedAction; клиенту реплика Ash про колени. Таскание балок с сарая должно реально давить вес (не `Weight=0.1` на бревне).
6. Только после TimedAction **success** на сервере: снять 4 гвоздя и 2 доски, молоток оставить.

Dedicated: не вешать логику только на `OnTick`. TimedAction + `OnClientCommand`. Поллер как в Safehouses, если понадобится файл — не для этой главы.

### Общий пролом vs флаг игрока (не путать)

Створка **одна на мир**. Глава 1 должна **закрываться у каждого** новичка Надзора, иначе второй зашедший навсегда сидит на c01.

| Ключ | Где | Смысл |
|------|-----|--------|
| `MeatballsNadzor_World.s00_gate_repaired` | мир | спрайт забит, ремонт больше нельзя |
| `MeatballsNadzor_s00_c01_done` | игрок | глава закрыта, **можно c02 Марго** |
| `MeatballsNadzor_s00_q01_gate_repaired` | игрок | алиас заказчика; писать вместе с `c01_done` |
| `MeatballsNadzor_World.s00_kennedy_state` | мир | `none` / `in_world` / `lost` |

**Поздний игрок:** `s00_gate_repaired == true` и `c01_done == false` → ветка Ash «уже забили» → сервер ставит `c01_done` **в том же тике диалога**, не «потом когда-нибудь». Без этого ТЗ сборка сломана.

**Кто чинил:** `c01_done` + жетон, если `kennedy_state == none` в момент success (первый).

Жетон не фармится второму. Лор 11 июля для опоздавших — реплика Ash + журнал, не второй металл.

### Триггеры завершения

1. Клиент: контекстное меню на объекте пролома → TimedAction (анимация ударов, звук молотка ванильный `Hammering` / аналог).
2. Прогресс 0..1 слать в UI (блок 3). Обрыв действия (орда, отход) — без съедания гвоздей.
3. Сервер success:
   - объект створки менять **только на сервере**, затем отдать клиентам ванильным пакетом (см. блок 4, спрайт);
   - мир `s00_gate_repaired` = true, `kennedy_state = in_world`;
   - чинившему: `c01_done` + `s00_q01_gate_repaired` = true;
   - `AddItem("MeatballsNadzor.KennedyDogTag")` только если `kennedy_state` был `none`;
   - `sendServerCommand`: `gateRepaired`, `showDogTagInspect` (inspect — только получившему жетон).
4. Реплика успеха Ash — радиус 8 от `8080,11750`.
5. Поздний игрок: диалог «уже забили» → `c01_done` без `AddItem`, `sendServerCommand` `c01LateComplete` (журнал, без бара ремонта).

Никаких `WorldDictionary` новых entity, если обходимся предметом + ModData.

### Тест-план (smoke)

- Без молотка / 3 гвоздя / 1 доска: действие не стартует, реплика отказа.
- Перегруз у створки: отказ.
- Успех: −4 nails −2 plank, hammer на месте, спрайт, флаги мира+игрока, один жетон.
- Второй персонаж при уже забитой створке: ремонта нет, Ash «уже забили», `c01_done == true`, можно говорить с Марго (c02). Жетона нет.
- Жетон уничтожен / ушёл с трупом: `kennedy_state = lost`, в столе офиса (и запасной ящик кухни) появляется `KennedyFieldReport`. Повторный металл не спавнится.
- Relog / новый клиент после ремонта: створка **забита** на экране, не выбита. Если выбита — сломан transmit.

---

## 4. Техриски сборки (обязательны к закрытию)

### 4.1 Уникальность ремонта vs прогресс новичка

Риск: мир-флаг закрыл тайл, игрок-флаг не выдан → старт мёртв.

Правило: **c02 Марго открывается от `c01_done` на персонаже, не от «я лично забивал».** Ветка Ash при `s00_gate_repaired == true` — не flavor, а гейт. Без записи `c01_done` в том же серверном ответе, что диалог, ТЗ не принято.

### 4.2 Жетон Кеннеди может исчезнуть из мира

Риск: первый починивший умер, вышел, сжёг, выкинул в воду — металл навсегда 0. Если Марго/караулка ждут предмет — квест ломается.

Правило:

- Физический `KennedyDogTag` — один на мир (`kennedy_state`).
- На `OnPlayerDeath` / если предмет не найден в инвентарях онлайн и не в известных контейнерах части после сейва: `kennedy_state = lost`.
- Тогда сервер кладёт **`MeatballsNadzor.KennedyFieldReport`** (углярная копия разведки, не второй жетон):
  - основной слот: контейнер стола офиса `8136, 11730, z=1`;
  - пока офис закрыт (до дня 100): тот же лист в ящике кухни Марго `8134, 11728, z=0`, чтобы Акт I не ждал капитана.
- Шанс: **гарантия один раз** при `lost`, не RNG «может появится». Повторно не клонировать. Inspect UI тот же паттерн, текст «копия, 11 июля».
- c02 **не** требует жетон в руках. Жетон/отчёт — лор и поздние главы. Застревание старта закрывает 4.1, не металл.

### 4.3 Спрайт пролома у только что зашедших

Риск: сервер думает «забито», клиент после join видит дыру (объект не уехал пакетом / не восстановлен из сейва).

Правило:

- Менять тайл **на сервере**. Не `setSprite` только локально на чинившем клиенте.
- После add/replace: `IsoGridSquare:transmitAddObjectToClients` / `IsoObject:transmitCompleteItemToClients` (или актуальный B42 эквивалент синхронизации `IsoObject`). Баррикада — ванильный API двери/`IsoThumpable`, чтобы ушёл тот же пакет, что у обычного заколачивания.
- На `OnLoad` / `OnServerStarted`: если `s00_gate_repaired` и на `8142,11752,0` нет баррикады — **восстановить объект**. Иначе relog и dedicated chunk unload вернут дыру.
- Тест: игрок A чинит, игрок B подключается на чистый клиент — створка закрыта до подхода.

Готово к сборке, когда три пункта 4.1–4.3 закрыты тестами из smoke, не «потом».

## 3. ТЗ агенту «мододел» — UI / UX

Ветка **`mod`**. Не панель. Ванильный стек: `ISPanel`, `ISUIElement`, `ISButton`, `ISLabel`, `ISScrollingListBox`, `ISProgressBar` (или полоса через `drawRect` как в ISInventoryPage). Без HTML, без кастомных шрифтов вне PZ. Текстуры — `media/ui/` мода, не воровать UI The Indie Stone пачкой.

### 3.1 Журнал смены «Записи дежурства»

Новое окно `ISNadzorDutyLog` (имя класса префикс Nadzor). Открытие: горячая клавиша или кнопка у Ash после выдачи c01.

- Заголовок: `Записи дежурства`.
- Список глав смены. Строка c01: `Дыра в караулке` + статус `открыто` / `створка держит` / `створка держит (не ты)` / `жетон найден` / `отчёт 11 июля`.
- Клик по строке: короткий текст из выдачи Ash (не простыня лора).
- Закрытие: ванильный `X`, Escape.
- Данные статуса только с сервера (ModData), клиент не «рисует победу» до флага.

Макет: как `ISQuestWindow` / journal mods — левая колонка список, правая текст. Отступы 4px, кнопки `ISButton` высотой как инвентарь.

### 3.2 Плашка ремонта створки

Пока идёт TimedAction у `8142,11752`:

- Маленький `ISPanel` якорь низ экрана / над hotbar, не перекрывать инвентарь целиком.
- Текст: `Створка ворот`.
- Прогресс-бар 0–100% от `action:getJobDelta()`.
- Срыв действия — панель `close()`.
- Успех — вспышка текста `Держит.` 2 сек, затем закрыть.

Не делать круговой MMO-каст. Прямой бар, как ремонт в ванили.

### 3.3 Осмотр жетона

По `showDogTagInspect` или ПКМ по предмету → `Inspect`:

- Окно `ISNadzorItemInspect`, размер ~280×220.
- Текстура жетона: металл, грязь, вмятина (одна PNG в `media/ui/nadzor_kennedy_tag.png`). Не градиенты в коде.
- Текст: `KENNEDY · KY NG` + строка «паз створки, 11 июля».
- Кнопка `Спрятать в карман` — закрывает UI, предмет уже в инвентаре (не клонировать).
- Escape = то же, что кнопка.

### 3.4 Согласование с ванилью

- Цвета: `getCore():getGoodHighlitedColor()` не радуга. Фон `0,0,0,0.8` как модалки PZ.
- Масштаб UI: `getCore():getScreenWidth()` / `UIManager` scale, не хардкод 1920.
- Не блокировать движение камеры сверх обычных модалок.
- RU строки в `media/lua/shared/Translate/RU/`, EN дубль.
- Скриншоты трёх окон — в описание PR мододела, не в git бинарём без нужды.

Готово, когда: журнал открывается, бар живой на ударах, inspect жетона без второй выдачи предмета, отказ без инструментов не показывает бар успеха.
